# Changelog

## Unreleased

- Backend: stricter CORS defaults, rate limiting, consistent JSON errors.
- Supabase: constraints + audit logs table/trigger.
- Desktop: Windows zip includes `config.json.example` + `RUN_ME_FIRST.txt`.

